[project]
name = "stage-2"
version = "0.1.0"
description = "Stage 2: TBD"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["stage_2"]
